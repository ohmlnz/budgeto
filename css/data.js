import { StyleSheet } from 'react-native';

export const stylesData = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center'
  },
  title: {
    marginTop: 10,
    marginBottom: 10,
    fontWeight: 'bold'
  },
  exceeds: {
    fontWeight: 'bold',
    color: '#B40404'
  }
});